terrain:
  width: 128
  height: 128

noise:
  type: "fbm" # perlin | fbm | simplex | billow | ridged
  scale: 25.0
  octaves: 4
  seed: 67
  height_scale: 15.0

output:
  mesh_dir: "meshes/"
  mesh_name: "terrain.obj"

render:
  cmap: "terrain" # colormap for matplotlib
